---
layout: layouts/base.njk
title: Upcoming Events
description: Join us for workshops, office hours, meetups, guest speakers, and hack nights at NJIT Job Club
eleventyNavigation:
  key: Events
---

{# Hero Section #}
<section class="relative overflow-hidden" style="background: linear-gradient(135deg, rgba(227, 195, 255, 0.3) 0%, rgba(255, 251, 255, 1) 25%, rgba(224, 242, 254, 0.4) 50%, rgba(255, 245, 245, 0.3) 75%, rgba(255, 251, 255, 1) 100%); padding-top: 140px; padding-bottom: 100px;">
  <div class="container">
    <div class="text-center max-w-4xl mx-auto">
      {# Badge #}
      <div class="inline-flex items-center gap-2 mb-6 px-4 py-2 rounded-full" style="background: rgba(103, 80, 164, 0.1); backdrop-filter: blur(10px);">
        <span class="material-icons-round" style="font-size: 1.125rem; color: var(--primary);">event</span>
        <span style="font-size: 0.9375rem; font-weight: 500; color: var(--primary);">Community Events</span>
      </div>
      
      {# Main Heading #}
      <h1 class="font-bold mb-6 animate-fade-in" style="font-family: 'Google Sans', sans-serif; color: #5E35B1; line-height: 1.1; font-size: clamp(3rem, 8vw, 4.5rem); letter-spacing: -0.02em;">
        Upcoming Events
      </h1>
      
      {# Subheading #}
      <p class="text-xl mb-8 leading-relaxed mx-auto" style="color: var(--text); opacity: 0.7; max-width: 700px; font-size: clamp(1.125rem, 2vw, 1.25rem); line-height: 1.6;">
        Connect, learn, and grow with the Job Club community through workshops, office hours, and networking events.
      </p>
      
      {# Event Type Filters #}
      <div class="flex flex-wrap gap-3 justify-center">
        <button class="event-filter active" data-filter="all" style="padding: 0.75rem 1.5rem; border-radius: 100px; border: 2px solid var(--primary); background: var(--primary); color: white; font-weight: 600; transition: all 0.3s;">
          All Events
        </button>
        <button class="event-filter" data-filter="workshop" style="padding: 0.75rem 1.5rem; border-radius: 100px; border: 2px solid var(--primary); background: transparent; color: var(--primary); font-weight: 600; transition: all 0.3s;">
          🛠️ Workshops
        </button>
        <button class="event-filter" data-filter="office-hours" style="padding: 0.75rem 1.5rem; border-radius: 100px; border: 2px solid var(--primary); background: transparent; color: var(--primary); font-weight: 600; transition: all 0.3s;">
          🕐 Office Hours
        </button>
        <button class="event-filter" data-filter="meetup" style="padding: 0.75rem 1.5rem; border-radius: 100px; border: 2px solid var(--primary); background: transparent; color: var(--primary); font-weight: 600; transition: all 0.3s;">
          🤝 Meetups
        </button>
        <button class="event-filter" data-filter="guest-speaker" style="padding: 0.75rem 1.5rem; border-radius: 100px; border: 2px solid var(--primary); background: transparent; color: var(--primary); font-weight: 600; transition: all 0.3s;">
          🎤 Guest Speakers
        </button>
        <button class="event-filter" data-filter="hack-night" style="padding: 0.75rem 1.5rem; border-radius: 100px; border: 2px solid var(--primary); background: transparent; color: var(--primary); font-weight: 600; transition: all 0.3s;">
          💻 Hack Nights
        </button>
      </div>
    </div>
  </div>
</section>

{# Events Grid Section #}
<section class="section-padding" style="background-color: #FEFBFF; padding-top: 80px; padding-bottom: 120px;">
  <div class="container">
    
    {# Loading State #}
    <div id="eventsLoading" class="text-center py-20">
      <div class="inline-block animate-spin rounded-full h-12 w-12 border-b-2" style="border-color: var(--primary);"></div>
      <p class="mt-4" style="color: var(--text); opacity: 0.6;">Loading events...</p>
    </div>
    
    {# No Events State #}
    <div id="noEvents" class="hidden text-center py-20">
      <span class="material-icons-round mb-4" style="font-size: 4rem; color: var(--primary); opacity: 0.3;">event_busy</span>
      <h3 class="text-2xl font-bold mb-2" style="font-family: 'Google Sans', sans-serif; color: var(--text);">No Events Found</h3>
      <p style="color: var(--text); opacity: 0.6;">Check back soon for upcoming events!</p>
    </div>
    
    {# Events Grid #}
    <div id="eventsGrid" class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
      {# Events will be dynamically inserted here #}
    </div>
  </div>
</section>

{# CTA Section #}
<section style="padding: 80px 0; background: linear-gradient(135deg, #6750A4 0%, #9575CD 100%); color: white;">
  <div class="container text-center">
    <h2 class="font-bold mb-6" style="font-family: 'Google Sans', sans-serif; font-size: clamp(2rem, 5vw, 3rem); line-height: 1.1;">
      Want to Host an Event?
    </h2>
    <p class="text-xl mb-8 max-w-2xl mx-auto" style="opacity: 0.9;">
      Have an idea for a workshop, talk, or community event? We'd love to hear from you!
    </p>
    <a href="mailto:kwilliams@njit.edu?subject=Event Proposal" class="btn inline-flex items-center gap-2" style="background: white; color: var(--primary); padding: 1rem 2.5rem; font-size: 1rem; border-radius: 100px; font-weight: 600;">
      <span class="material-icons-round">email</span>
      <span>Propose an Event</span>
    </a>
  </div>
</section>

<style>
  .event-filter:hover {
    transform: translateY(-2px);
    box-shadow: 0 4px 12px rgba(103, 80, 164, 0.2);
  }
  
  .event-filter.active {
    background: var(--primary) !important;
    color: white !important;
  }
  
  .event-card {
    background: white;
    border-radius: 24px;
    overflow: hidden;
    box-shadow: 0 4px 20px rgba(103, 80, 164, 0.08);
    transition: all 0.3s ease;
    border: 1px solid rgba(0, 0, 0, 0.04);
  }
  
  .event-card:hover {
    transform: translateY(-8px);
    box-shadow: 0 12px 40px rgba(103, 80, 164, 0.15);
  }
  
  .event-type-badge {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.5rem 1rem;
    border-radius: 100px;
    font-size: 0.875rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.05em;
  }
  
  .event-type-workshop { background: rgba(103, 80, 164, 0.1); color: #6750A4; }
  .event-type-office-hours { background: rgba(0, 191, 165, 0.1); color: #00BFA5; }
  .event-type-meetup { background: rgba(255, 111, 0, 0.1); color: #FF6F00; }
  .event-type-guest-speaker { background: rgba(156, 39, 176, 0.1); color: #9C27B0; }
  .event-type-hack-night { background: rgba(33, 150, 243, 0.1); color: #2196F3; }
  
  @keyframes fade-in {
    from {
      opacity: 0;
      transform: translateY(20px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }
  
  .animate-fade-in {
    animation: fade-in 0.6s ease-out;
  }
</style>

<script src="/js/events.js"></script>
